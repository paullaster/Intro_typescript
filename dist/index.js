"use strict";
//Arrays in typescript should be of the same type: but they also use union type
let numArray = [1, 2];
numArray.forEach((el) => {
    console.log("Item " + el);
});
//tuples:
let t1 = [1, "ochieng"];
;
let s1 = 7 /* Sizes.Small */;
console.log(s1);
